# some reason first expression gets ignored??
# its not just a line thing

import os
import socket

a = b + c
y, z = mx + c
c *= a

print(a, b, c, d)

sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
socket.socket(socket.AF_INET, socket.SOCK_STREAM)

class A:

    def function_a(name, age):
        None


    def function_b(self, arg1, arg2):
        None

    def function_c():
        None
